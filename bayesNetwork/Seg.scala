package bayesNetwork

import java.text.{DateFormat, SimpleDateFormat}
import java.util.Calendar

/**
  * Created by Asia on 2017/7/27.
  */
object Seg {
/*  def cut(paraString:String): Array[String] ={
    val tmp=paraString.split(",");
    var result=new Array[String](3);
    result(0)=tmp(3);
    result(1)=tmp(4).toInt match {
      case 1|2|3=>"short"
      case 4|5|6=>"middle"
      case 7|8|9=>"long"
      case 0=>"none"
      case _=>"inf"
    }//Of match cut the dis
    val date=new SimpleDateFormat("yyyyMMdd")
  }//Of cunt*/
}//Of objectSeg
